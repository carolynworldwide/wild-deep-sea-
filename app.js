var Photo = function(fileLocation); {
	this.path = fileLocation;
	this.votes = 0;
}

tracker.method = function() {
  var this.name;
  var this.display;
 	var trackerPhotos[];
 	 };

var randImg = function () {
	return Math.floor(Math.random() * photoFiles.length); //will generate 0-11 array indices for 1-12 objects

tracker.method 2 = function() {
  lets visitor vote
};

//when app loads, randomly selects two deep sea creatures from deep sea img file
	//ARRAY
//function Image = DeepSea(title, creature);

document.getElementById("photoGen1").src = randomPhoto1["src"];
var title1 = document.getElementById("photoName1");

var SeaCreatures = function(name) {
		this.name = name;
}

var borgTube = new SeaCreature('borgTube');
var deepSeaJellyfish = new SeaCreature('deepSeaJellyfish');
var dumboOctopus = new SeaCreature('dumboOctopus');
var frillShark = new SeaCreature('frillShark');
var longFaceFish = new SeaCreature('longFaceFish');
var orangeJaws = new SeaCreature('orangeJaws');
var pinkSalamander = new SeaCreature('pinkSalamander');
var satchmoFish = new SeaCreature('satchmoFish');
var seaCucumber = new SeaCreature('seaCucumber');
var toothFish = new SeaCreature('toothFish');
var whaFish = new SeaCreature('whaFish');
var whoKnowsWhat = new SeaCreature('whoKnowsWhat');

function SeaCreature(name) {
	this.SeaCreature = SeaCreature;
}

}

var addImages = function () { //declare function 
	var img1Loc = document.getElementById('img1')//access DOM function
	var img2Loc = document.getElementById('img2');
	if (img1Loc.firstChild) {
			img1Loc.removeChild(img1Loc.firestChild);

//---------------------------------------------------------------
	}
//image constructor
function Image(src, title, movie) {
	this.src = src;
	this.title = title;
	this.movie = movie;

//array of images
var evilCharacters = []

//insert random photo 1
var random1 = randomNumber();
var randomPhoto1 = (evilCharacters[random1]);
console.log(random1);
//populating the DOM with an image
document.getElementById("photoGen1").src = randomPhoto1["src"];
var title1 = document.getElementById("photoName1");
//insert random photo 2

//calling photo generator functions
randomImg1();
randomImg2();
for (var 1 = 0; i < 2; i++) {
	randomImg(i);//duplicate photo error correction
}
//voting system event listener
}
//----------------------------------------------------------------

//HTML table id="battle">

var SeanBean = function(path, character, movie, death) {
	this.path = path;
	this.character = character;
	this.movie = movie;
	this.death - death;
	this.write = function(
		var msg = '<img src=')
}

//----------------------------------------------------------------
//do while runs at least once, can will run again
addImages(); //eventListener
var picButton = document.getElementById('picButton');
picButton.addEventListener('click', addImages);

//ask visitor to vote on which creature is the weirdest
	//PROMPT ALERT BUTTON

//after voting, highlight winning deep sea photo
	//IF ELSE

//chart relative popularity of these two photos 
			//(start with the Chart.js API)

//increase total votes for winning photo by one
//FOR LOOP

//ask visitor to vote for two new random deep sea photos
//PROMPT ALERT BUTTON

//------------------NOTES--------------------------------

//A set of Photo objects to manage all the attributes and behaviors around kitten photos.
//A single Tracker object to manage all the attributes and behaviors around a visitor's voting experience.

//User voting on images = primary interactivity
//Storing running vote totals/displaying in chart(s) using Chart.JS = primary technical challenge
